version: "3.9"
services:
  web:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
    environment:
      NODE_ENV: development
      CHOKIDAR_USEPOLLING: "true"
    command: ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
    restart: always
